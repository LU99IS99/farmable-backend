{
    "name": "farmable-backend",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      
      "build": "tsc",
  
      "deploy": "wrangler deploy",
  
      "build-and-deploy": "npm run build && npm run deploy",
  
      "build:ci": "npm run build && npm run deploy"
    },
    "devDependencies": {
      "@cloudflare/workers-types": "^4.20250129.0",
      "typescript": "^4.9.5",      
      "wrangler": "^3.28.1"
    },
    "dependencies": {
      "hono": "^4.7.1"           
    }
  }
  